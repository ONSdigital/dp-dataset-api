---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golangci/golangci-lint
    tag: v1.64.8-alpine

inputs:
  - name: dp-dataset-api

run:
  path: golangci-lint
  args:
    - run
    - ./...
  dir: dp-dataset-api

